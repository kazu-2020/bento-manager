<!DOCTYPE html>
<html data-theme="bumblebee">
  <head>
    <%= render "shared/head" %>
  </head>

  <body class="min-h-screen bg-base-200">
    <div class="drawer lg:drawer-open">
      <input id="main-drawer" type="checkbox" class="drawer-toggle">

      <div class="drawer-content flex flex-col">
        <%= component "navbar", title: content_for(:title) %>

        <main class="flex-1 p-2 sm:p-6">
          <div class="max-w-6xl mx-auto">
            <%= yield %>
          </div>
        </main>
      </div>

      <div class="drawer-side z-40">
        <label for="main-drawer" aria-label="メニューを閉じる" class="drawer-overlay"></label>
        <%= component "sidebar", current_path: request.path %>
      </div>
    </div>

    <turbo-stream-modal-container></turbo-stream-modal-container>
    <div id="toast-container" class="toast toast-start toast-bottom z-50" aria-label="通知">
      <% if notice.present? %>
        <div class="toast-item">
          <%= component "toast", message: notice, type: :success %>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="toast-item">
          <%= component "toast", message: alert, type: :error %>
        </div>
      <% end %>
    </div>
  </body>
</html>
