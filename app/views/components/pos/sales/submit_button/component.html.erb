<div id="sale-submit-button">
  <%# 顧客区分選択 %>
  <div class="flex gap-3 mb-3">
    <label class="flex-1 cursor-pointer">
      <input type="radio" name="cart[customer_type]" value="staff"
             class="hidden peer"
             <%= "checked" if staff_selected? %>
             data-action="change->pos-cart#quantityChanged">
      <div class="card border-2 peer-checked:border-primary peer-checked:bg-primary/10 border-base-300 transition-all">
        <div class="card-body p-3 text-center">
          <span class="text-2xl">👔</span>
          <span class="font-medium"><%= t(".staff") %></span>
        </div>
      </div>
    </label>

    <label class="flex-1 cursor-pointer">
      <input type="radio" name="cart[customer_type]" value="citizen"
             class="hidden peer"
             <%= "checked" if citizen_selected? %>
             data-action="change->pos-cart#quantityChanged">
      <div class="card border-2 peer-checked:border-primary peer-checked:bg-primary/10 border-base-300 transition-all">
        <div class="card-body p-3 text-center">
          <span class="text-2xl">🏠</span>
          <span class="font-medium"><%= t(".citizen") %></span>
        </div>
      </div>
    </label>
  </div>

  <%# 販売確定ボタン %>
  <button type="submit"
          class="btn btn-primary btn-lg w-full"
          <%= "disabled" if disabled? %>>
    <% if has_items_in_cart? %>
      <%= t(".submit_with_total", total: final_total_display) %>
    <% else %>
      <%= t(".submit_label") %>
    <% end %>
  </button>
</div>
