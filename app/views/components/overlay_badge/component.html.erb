<%# バッジは装飾扱い。pointer-events-none で下のカードリンクへクリックを素通しする %>
<% if overlaid? %>
  <div class="indicator w-full">
    <div class="indicator-item indicator-center indicator-middle z-10 pointer-events-none">
      <%= badge %>
    </div>
    <%= content %>
  </div>
<% else %>
  <%= content %>
<% end %>
