<%# Sidebar - サイドバーメニュー %>
<aside class="bg-base-200 min-h-full w-72 flex flex-col">
  <%# モバイル用ヘッダー %>
  <div class="lg:hidden p-4 border-b border-base-300">
    <div class="flex items-center gap-2">
      <span class="text-2xl">🍱</span>
      <span class="font-bold text-lg bg-gradient-to-r from-amber-600 to-orange-500 bg-clip-text text-transparent">
        Bento Manager
      </span>
    </div>
  </div>

  <%# メインメニュー %>
  <ul class="menu p-4 text-base-content flex-1">
    <li class="menu-title text-xs uppercase tracking-wider text-base-content/60 mb-2">
      メインメニュー
    </li>

    <% menu_items.each_with_index do |item, index| %>
      <% if index == 1 %>
        <li class="menu-title text-xs uppercase tracking-wider text-base-content/60 mt-6 mb-2">
          管理
        </li>
      <% end %>

      <li>
        <%= link_to item.path, class: menu_item_class(item) do %>
          <%= render_icon(item.icon) %>
          <span><%= item.label %></span>
        <% end %>
      </li>
    <% end %>
  </ul>

  <%# フッター %>
  <div class="p-4 border-t border-base-300 text-xs text-base-content/50">
    <p>&copy; <%= Date.current.year %> Bento Manager</p>
  </div>
</aside>
