<%= modal_stream_show do %>
  <dialog class="modal">
    <div class="modal-box">
      <h3 class="text-lg font-bold mb-4"><%= t("locations.new.title") %></h3>

      <%= render "shared/modal_close_button" %>

      <%= render "modal_form", location: @location %>
    </div>

    <%= render "shared/modal_backdrop" %>
  </dialog>
<% end %>
