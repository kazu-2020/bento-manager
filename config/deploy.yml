# Name of your application. Used to uniquely configure containers.
service: bento_manager

# Name of the container image (use your-user/app-name on external registries).
image: bento_manager

# Deploy to these servers.
servers:
  web:
    - 100.68.213.116
  # job:
  #   hosts:
  #     - 192.168.0.1
  #   cmd: bin/jobs

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# If used with Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
#
# Using an SSL proxy like this requires turning on config.assume_ssl and config.force_ssl in production.rb!
#
# Don't use this when deploying to multiple web servers (then you have to terminate SSL at your load balancer).
proxy:
  ssl: true
  host: x220-158-17-38.static.xvps.ne.jp

# Where you keep your container images.
registry:
  # Alternatives: hub.docker.com / registry.digitalocean.com / ghcr.io / ...
  server: localhost:5550

  # Needed for authenticated registries.
  # username: your-user

  # Always use an access token rather than real password when possible.
  # password:
  #   - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - SECRET_KEY_BASE
    - SENTRY_DSN
    # 日次リストア訓練が app コンテナ内で `litestream restore` を叩くため、
    # accessory だけでなく app 側にも S3 の認証情報が要る（ADR-0001 決定 5）。
    - AWS_ACCESS_KEY_ID
    - AWS_SECRET_ACCESS_KEY
  clear:
    # リストア訓練で許容する「復元側が本番より遅れている件数」。
    # 訓練の実行中にも売上が入りうるため、厳密一致では営業中に誤検知する（ADR-0001 決定 6）。
    RESTORE_DRILL_TOLERANCE: 5

    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: true

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # Match this to any external database server to configure Active Record correctly
    # Use bento_manager-db for a db accessory server on same machine via local kamal docker network.
    # DB_HOST: 192.168.0.2

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "bento_manager_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/vite/assets


# Configure the image builder.
builder:
  arch: amd64

  args:
    # 書く側（accessory の image）と戻す側（app イメージ内のバイナリ）でバージョンが
    # ずれないよう、このファイルを唯一の出所にする。Dockerfile 側の ARG のデフォルトは
    # kamal を通さない `docker build` 用のフォールバック。
    LITESTREAM_VERSION: 0.5.16

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: ruby-3.4.5
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

# Use accessory services (secrets come from .kamal/secrets).
accessories:
  # ADR-0001 決定 3: Litestream は app に -exec で同梱せず、独立コンテナとして動かす。
  # -exec なら「バックアップが壊れたら Rails も起動しない」という強い保証が得られるが、
  # それは「バックアップが壊れたら店が売上を記録できなくなる」ことを意味する。
  # バックアップを 1 日失うのは復旧可能だが、営業中に POS が上がらないのは復旧不能。
  litestream:
    # バージョンは builder.args.LITESTREAM_VERSION と揃えること
    image: litestream/litestream:0.5.16
    # アドレスを直書きせず web ロールに追従させる。同じ volume を共有する以上
    # app と別のホストに置く意味はなく、サーバー移設時に片方だけ古いアドレスが
    # 残ると、レプリケーションだけが旧サーバーで動き続ける
    roles:
      - web
    cmd: replicate
    options:
      # app コンテナと同じ uid に揃える。root で動かすと Litestream が作る
      # -shm / -wal などの所有者が app とずれる（ADR-0001 決定 3）。
      user: 1000:1000
      restart: always
    env:
      secret:
        - AWS_ACCESS_KEY_ID
        - AWS_SECRET_ACCESS_KEY
    files:
      - config/litestream.yml:/etc/litestream.yml
    volumes:
      # app コンテナと同じ named volume を共有し、同じファイルを参照させる
      - bento_manager_storage:/rails/storage
