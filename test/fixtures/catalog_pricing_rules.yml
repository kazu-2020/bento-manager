# CatalogPricingRule fixtures for testing
# price_kind enum: regular: 0, bundle: 1
# trigger_category: "bento" or "side_menu" (String)

salad_bundle_by_bento:
  target_catalog: salad
  price_kind: 1
  trigger_category: "bento"
  max_per_trigger: 1
  valid_from: <%= 1.month.ago.to_date %>
  valid_until:
