<div class="p-8 bg-neutral min-h-screen">
  <div class="flex flex-wrap gap-6">
    <% IconsPreview.icon_names.each do |icon_name| %>
      <%= render Icon::Component.new(name: icon_name, size: :lg, extra_class: "text-neutral-content") %>
    <% end %>
  </div>
</div>
