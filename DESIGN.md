---
version: alpha
name: bento-manager
description: 役場等への出張訪問販売を行うお弁当屋さんの業務アプリ。屋外・モバイル・片手操作を前提とする。
colors:
  primary: "oklch(56% 0.19 40)"
  secondary: "oklch(51% 0 0)"
  neutral: "oklch(26% 0.085 40)"
  info: "oklch(48% 0.14 260)"
  success: "oklch(48% 0.10 171)"
  warning: "oklch(54% 0.12 85)"
  error: "oklch(43.4% 0.170 28.3)"
  staff: "oklch(40% 0.12 55)"
  citizen: "oklch(45% 0.10 240)"
typography:
  body:
    fontFamily: "Noto Sans JP"
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.5
  mono:
    fontFamily: "Noto Sans Mono"
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.5
rounded:
  field: 0.75rem
  box: 0.75rem
  selector: 2rem
spacing:
  base: 0.25rem
---

# DESIGN.md

このファイルは、UI コードを書くエージェントに対して「**いつどのトークンを選ぶか**」を伝えるためのものである。
用語は `CONTEXT.md` に従う。クラス名の書き方など技術的な制約は `.claude/rules/css-framework.md` にある。

## Overview

母がひとりで使う業務アプリである。使う場所は役場の駐車場や販売の現場で、立ったまま、多くの場合は片手で操作する。
このことが視覚設計のほとんどを決めている。押し間違いが売上の記録ミスに直結するため、主要な操作は大きく、迷いの余地なく置く。

パレットはコメダ珈琲店の公開デザイン仕様を参考にしている。ブランドオレンジとディープレッド、
純黒を避けたソフトブラック、温かいクリームの面。コメダの原則である「オレンジは差し色に。広い面積に使わない」も踏襲する。

装飾は目的ではない。母が「次に何を押せばよいか」を一目で判断できることだけが基準である。

## Colors

**値の出典はこのファイルではなく [`app/frontend/stylesheets/application.tailwind.css`](app/frontend/stylesheets/application.tailwind.css) である。**
daisyUI のテーマ `bento` がすべてのトークンを定義しており、上の frontmatter はその写しにすぎない。
色を変えるときは CSS を変え、このファイルを追随させる。逆方向はしない（理由は ADR-0009）。

色は 2 つの層に分かれている。**この 2 つを混同しないこと。**

### 意味の層 — 入れ替えてはいけない

ドメインの概念に固定されている。見やすさや好みを理由に変更してはならない。

| トークン | 意味 |
| --- | --- |
| `staff` | 顧客タイプの「関係者」 |
| `citizen` | 顧客タイプの「一般」 |
| `success` | 有効・提供中の状態 |
| `error` | **もう選べない状態**（提供終了・閉鎖・無効化・売り切れ・選択不可）。加えて後戻りしにくい操作と「返金」 |
| `warning` | 条件付き・これからの状態（期間前のクーポン、弁当が必要） |
| `info` | 案内・補足情報。加えて差額精算の結果が「追加徴収」であること |

`error` は失敗を意味しない。「終わっている / もう選べない」を意味する。
売り切れは `CONTEXT.md` の定義どおり正常な状態であって、エラーではない。
**操作の失敗を伝えるのは toast（`#toast-container` の alert）の役目**であり、バッジではない。

### 役割の層 — UI の重み付け

| トークン | 役割 |
| --- | --- |
| `neutral` | フォームの送信ボタン、新規作成への導線。**この画面でいちばん押してほしいもの**。1 画面に 1 つ |
| `primary` | **選ばれているもの**。選択中のタブ、カート投入済みの商品、選択済みの在庫項目、空状態からの誘導 |
| `secondary` | 現在どこでも使っていない。使うなら先に役割を決めてここに書くこと |
| `accent` | **退役した。使わない**（下記） |

`neutral` が主要ボタンなのは、**このテーマで最も明度が低く、白文字に対して 15.78:1 と最も強いから**である。
オレンジ（`primary`, 4.76:1）より確実に目立つ。彩度ではなく明度で勝っている点に注意。

無彩色の黒（`#333`）ではなく焦げ茶（`#420e00`）なのは、**全幅の塗りにしたとき無彩色だけがパレットから浮くため**である。
コメダが純黒を避けて `#333333` を使うのは文字のためであって、大面積の塗りの話ではない。
なお明度を下げた分だけ彩度を足せるので、`staff`（顧客タイプの茶）との色差はむしろ広がっている（ΔE 0.144 → 0.152）。
コメダの「オレンジを広い面積に使わない」とも整合する。

### `accent` は退役している

`accent` はどこからも参照していない。`badge-accent` / `text-accent` / `btn-accent` を新たに書かないこと。
テーマ上は `primary` と同じ値の別名にしてあるので、誤って使っても浮いた色にはならないが、意味は持たない。

退役の理由は色域の制約である。`accent` は塗り（白文字が乗る）と文字（カード上で読める）の両方で使われていたが、
**その両方で AA を満たす暖色は存在しない**（探索して残ったのは紫・マゼンタ・濃緑だけだった）。
実際 `text-accent` は 2.61:1 で AA を割っていた。

かつての用途は次のように置き換えた。

| かつての用途 | 置き換え先 |
| --- | --- |
| 顧客タイプ（関係者）のバッジ | `bg-staff text-white` / `bg-citizen text-white` |
| 「セット」の印 | `badge badge-xs badge-outline` |
| 追加発注の合計個数 | `badge badge-outline` |
| 追加発注の「+N個」・割引額 | 太字（`font-bold` / `font-medium`）と `base-content` |

**数量と金額は色ではなく字の太さで強調する。** 屋外で最初に失われるのは彩度であって字の太さではない。

### 選択状態の表し方

「選ばれている」ことは `primary` で表す。カートに入っている商品、選択済みの在庫項目のいずれも
`border-primary bg-primary/10` を使う。

ただし**顧客タイプの選択だけは例外**で、選択中の側を `staff` / `citizen` 色そのもので塗る
（`peer-checked:bg-staff peer-checked:border-staff peer-checked:text-white`）。
顧客タイプという 1 つの概念に専用トークンがあるのに、選択状態だけ第 3 の色を使う理由がないためである。
加えてこのトグルは販売確定ボタンのすぐ上に全幅で並ぶので、彩度の高いオレンジを置くと主役と競合する。

`bg-primary/10` の淡い塗りは素の面との差が 1.09:1 しかなく、実際にはほとんど見えない。
**選択を伝えているのは枠線である**（`border-primary` は 4.76:1）。塗りだけに頼らないこと。

### 差額精算の 3 つの結果

`Pos::Refunds::SubmitButton` は結果に応じて色を変える。

| 結果 | 色 | 理由 |
| --- | --- | --- |
| 追加徴収 | `btn-info` | 現金を受け取る |
| 等価交換 | `btn-success` | 現金が動かない |
| 返金 | `btn-error` | **現金が手元から出ていく** |

`CONTEXT.md` は 3 つを対等に扱うが、それは金額の扱いの話である。
現金の向きで見ると返金だけが非対称で、屋外で片手で操作する状況では、現金が出る操作だけ色が違うことに意味がある。
`error` の「もう選べない / 後戻りしにくい」という意味とも矛盾しない。

## Typography

Noto Sans JP。日本語のみを扱うため、欧文フォントとの併用は考えなくてよい。

サイズは daisyUI / Tailwind の既定に従い、独自のスケールは持たない。
`text-[10px]` のような任意値は、バッジ内の補助的な数値など、既定のスケールで収まらない場所に限って使う。

## Layout

**モバイルが基準である。** ブレークポイントは常にモバイルから上に広げる向きで書く。

- サイドバーは `lg:drawer-open` で、デスクトップでのみ常時展開する。モバイルでは drawer に収まる
- ページ背景は `base-200`、カード面は `base-100`。**モバイルではカードが全幅になり背景がほとんど見えない**ため、温かみは `base-100` 側に持たせてある
- 本文の余白は `p-2 sm:p-6`。狭い画面では余白を削って情報量を優先する
- 本文の最大幅は `max-w-6xl`
- 主要な操作ボタンは `w-full`。片手の親指で押せる幅を確保する
- 画面下端に固定するものは `safe-area-pb` を使う（iPhone のホームインジケータ）

## Elevation & Depth

daisyUI テーマの `--depth: 1` に任せる。独自の影を定義しない。
`--depth: 0`（完全なフラット）にすると入力欄の枠が消える。フラット化しないこと。

`base-100` と `base-200` の輝度比は 1.04:1 しかない。背景を淡いオレンジに保つ以上、
塗りの明度差でこれ以上稼ぐことはできない（`base-200` を下げると `primary` のオレンジ文字が AA を割る）。
そのため**面の分離は塗りではなく枠線が担っている**。`base-300` は枠線専用のトークンで、
`base-100` に対して 2.04:1 ある。屋外の直射日光では彩度と淡い影が先に失われるが、輝度差のある枠線は残る。

**`base-300` をこれ以上暗くしないこと。** UI 境界の目安である 3:1 に到達させることは可能だが
（`oklch(64% 0.082 70)` で 3.21:1）、そうすると未選択の枠が強くなりすぎ、
選択中（`border-primary`）が未選択より強く見える倍率が 2.3 倍から 1.5 倍まで落ちる。
押せることは行内のチェックボックスが伝えているので、**選択の際立ちを優先する**。

`base-300` を塗りに使わないこと。暗くしてあるため本文とのコントラストが足りない。

モーダルは `Modal::Component` に集約されており、殻を個別に組まない。

## Shapes

`--radius-field`（ボタン・入力・タブ）と `--radius-box`（カード・モーダル）はどちらも `0.75rem` で揃えてある。
ピル型（`2rem`）は入力欄が「入力できる場所」に見えなくなるため使わない。

`--border` は `1px`。daisyUI では badge / btn / input / select / tab / alert / card など 20 以上の
コンポーネントがこの 1 つのトークンを参照するので、変えるとアプリ全体の枠線が一律に動く。
**枠が見えにくい・強すぎるという話が出たら、色（`base-300`）の前にこの太さを見ること。**
個別に `rounded-*` を指定するのは、daisyUI コンポーネントの外側で独自の面を作るときに限る。

## Components

daisyUI のコンポーネントを優先する。ボタン・カード・フォーム・バッジ・モーダルは daisyUI のクラスで組む。

### btn

| クラス | 用途 |
| --- | --- |
| `btn-neutral` | フォーム送信、新規作成への導線。1 画面に 1 つが原則 |
| `btn-ghost` | 低優先度の操作、アイコンボタン、キャンセル |
| `btn-outline btn-primary` | 空状態からの誘導、ナビゲーション |
| `btn-primary` | `join` によるセグメント切り替えの選択中（非選択は `btn-ghost`）。**タブには使わない**（下記） |
| `btn-error` | 後戻りしにくい操作（提供終了、返品への導線） |

`btn-primary` を `w-full` で使わないこと。全幅のオレンジは `btn-neutral` と主役を奪い合う。

タブは daisyUI の `tabs tabs-lift` + `tab` / `tab-active` で組む。`btn-*` は使わない。
`btn-primary` が当てはまるのは弁当販売分析の期間切り替え（`join` グループ）だけである。

### badge

状態を表すバッジは `badge-soft` を伴う。`badge-success` / `badge-error` / `badge-warning` の 3 つで状態を表し、上の「意味の層」に従う。
数量やセット価格の印は `badge-neutral badge-outline` を使う。
色クラスを省いた素の `badge-outline` は文字色を持たず親から継承するため、
薄い文字色のブロックに置くと枠ごと消える。色は必ず添える。

`status_badge` はドメインごとに存在する（`discounts` / `locations` / `catalogs`）。新しい状態バッジを作るときは既存の 3 つと同じ対応表を使う。

## Do's and Don'ts

### Do

- daisyUI のコンポーネントを最初に探す。無いものだけ Tailwind のユーティリティで組む
- ブレークポイントはモバイルを基準に、`sm:` `lg:` で上に広げる
- 主要な操作は `w-full` で幅いっぱいに置く
- 状態を表す色は「意味の層」の表を引く。見た目で選ばない
- 選択状態は塗りではなく**枠線**で伝える

### Don't

- **淡い `warning` だけで気づかせようとしない。** `badge-warning badge-soft` / `text-warning` は
  可読性としては AA を満たす（それぞれ 4.77:1 / 4.80:1）が、`warning` の色相 85 は `base` 系の 74 と
  わずか 11° しか離れておらず、淡くすると地色のクリームに溶けて**目に入らない**。
  `success`（171）と `error`（28）は淡くしても残るので、`warning` だけが構造的に不利である。
  見落とすと困る場面では、アイコン・位置・文言など色以外の手掛かりを併用すること
- **`data-theme` に `bento` 以外の名前を書かない。** ビルドに登録されているテーマは `bento` だけで、他の名前を書いても黙って `bento` にフォールバックする
- **`dark:` を使わない。** このアプリはライト固定である（`color-scheme: "light"`, `prefersdark: false`）。ダークモードは対応範囲外
- **意味の層の色を見やすさのために変えない。** 売り切れが赤いのは意図であって、エラーの誤用ではない
- **`accent` を使わない。** 退役済みのトークンである。選択は `primary`、数量・金額の強調は字の太さで表す
- `application.tailwind.css` にカスタム CSS を足さない。daisyUI と Tailwind で組めないか先に確かめる
- このファイルの frontmatter を色の出典として扱わない。出典は常に CSS 側である
